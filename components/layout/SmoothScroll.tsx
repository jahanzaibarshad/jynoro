'use client'

/** Native scroll — Lenis removed for smoother performance with scroll-linked effects */
export default function SmoothScroll({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
